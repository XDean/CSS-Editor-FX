package xdean.css.editor.context.setting.model;

import javafx.scene.input.KeyCombination;

public class CssEditorActionKeyOption extends CssEditorKeyOption<Void> {
  public CssEditorActionKeyOption(String key, KeyCombination defaultValue) {
    super(key, defaultValue);
  }
}
